#!/bin/sh
#
# small script to execute automated software updates
#

#
# paranoia settings
#

umask 022

PATH=/sbin:/bin:/usr/sbin:/usr/bin
export PATH

zypper -q -n ref || echo "Something went wrong updating the repos. Exit..." ; exit 1
zypper -q -n up || echo "Something went wrong updating the packages."


